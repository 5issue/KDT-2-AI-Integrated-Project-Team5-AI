# AGENTS.md

> AI 에이전트(Mavis / opencode / codex / claude code 등)가 이 레포 작업할 때 따라야 할 규칙.
> 이 파일은 **프로젝트 한정**. 다른 레포에는 적용하지 말 것.
> (인간) 이 파일외에 .cursorrules, CLAUDE.md 등 필요시 추가 가능

## 기본 정보

- **메인 브랜치**: `main`
- **PR 워크플로우**: GitHub Pull Request (front-end → openLeeWorld 리뷰)

## 코드 변경 규칙

### 0. Security and Compliance Rules

- NEVER disclose or leak passwords, private API keys, or database credentials.
- Mask any PII (Personally Identifiable Information) before processing.
- Always ask for human confirmation before executing any shell commands or modifying system files.
  **무엇이 "확인이 필요한" 명령인지는 아래 12절의 다섯 문항이 정합니다** (2026-09-16 추가).
  12절이 이 줄보다 **구체적이고 우선합니다** - 다섯 문항이 전부 '아니오' 인 명령
  (읽기 전용 조회, 로컬 분석, 테스트, 레포 안 파일 수정, `uv run pyright`/`ruff` 같은 정적 체크)은
  확인 없이 실행합니다. 하나라도 '예' 면 이 줄대로 먼저 묻습니다.
- Refer to these security protocols for every code generation task.

### 1. PR 설명은 8섹션 템플릿 사용

- 자세한 형식: [.github/pull_request_template.md](.github/pull_request_template.md) 참조
- **순서 고정**: 요약 → 작업 내용 → 걸린 시간 → 리뷰 요청 → 고민한 점 → 테스트 실행 → 참고사항 → 시각 자료
- **발표 직전 PR**: 4섹션 (리뷰 요청) 에 "지금 경량화 중이라 백엔드 배포는 필수가 아님" 같은 양보 표현 OK
- **이모지**: 본문/코멘트/PR 제목 = 0개. 단 6섹션 (테스트 실행 여부) 의 👍/🙅/🤯 이모지는 원본 형식 보존.

### 2. 커밋 메시지 / PR 제목 톤

- "우리 추천", "강력 권장", "의견 있으시면 코멘트 부탁드립니다" 식 collaborative
- "절대 XXX", "불가", "양보 불가" 식 dictatorial 금지
- 예시: `feat: VT verdict 카드 UI 추가` (O) / `절대 추가해야 함` (X)

### 3. PR 자동 커밋/PR 금지

- AI 에이전트가 사용자 대신 git commit / push / PR 생성 X
- 사용자가 명시적으로 "자동으로 해" 라고 하기 전까지 절대 금지
- 작업 후 보고만: 변경 파일 / 검증 결과 / 다음 단계

## 코딩 규칙

### 4. 중국어 / 일본어 한자 사용 금지

- 이 프로젝트는 한국어 + 영문 + 숫자만 사용
- `rg '[\u4e00-\u9fff\u3040-\u309f\u30a0-\u30ff]'` (또는 python regex) 로 검색해서 0건 확인
- 발견 시 한국어로 교체 (예: `候选` → `후보`, `链接` → `링크`, `フィッシング` → `피싱`)

### 5. 잔재 typecheck / lint 에러는 함께 픽스

- 발표/마감 직전이면 build 깨진 상태로 두면 안 됨
- 거대한 리팩토링은 손대지 말 것 (안정 > 깔끔), 사용자가 지시한 리팩토링 수행

## 작업 패턴

### 7. 작은 단위로 끊기

- 가능하면 작업 단위 한번에 5~6개 파일 이내로 변경하고 최대 10개 넘지 않도록
- 작업 끝나면 검증 한 번에 통과시키고 보고
- "작업 → 검증 → 보고" 사이클을 짧게 반복

### 8. PR 작성 전 확인

- [ ] PR 설명 8섹션 채웠는가?
- [ ] 시각 자료 (스크린샷) 첨부했는가? (선택)
- [ ] 검증 3종 통과했는가?
- [ ] 중국어/한자 0건인가?
- [ ] `main` 브랜치와 충돌 0인가?

## 9. 타입 시스템 및 린팅

- 이 프로젝트는 **엄격한 파이썬 타입 힌트(Strict Type Hinting)**를 준수합니다.
- 코드를 변경하거나 새로 작성할 때는 반드시 Pylance/Pyright 기준에 맞게 모든 매개변수와 반환값의 타입을 명시해야 합니다.
- 파일 수정 도구(Edit tool)를 사용한 후에는 아래 명령어를 실행하여 타입 에러가 없는지 자발적으로 검증하세요.
- 타입 검사 명령어: `uv run pyright` (cd <해당 폴더 이동 후>)

## 10. AI 코딩 도구의 권한

ai 코딩 도구는 특정 아키텍쳐나 llm api 모델 등 비용이나 보안과 관련된 결정사항 및 변경사항은 사용자의 검토 및 지침을 받고 실행하는 것을 기본으로 합니다. 비즈니스 로직을 구현하기 위한 로컬 파일 변경 외의 보안, 비밀 관련 파일 열람이나 인프라 변경 등은 기본적으로 허용하지 않습니다.

### 10-1. "비밀 관련 파일" 은 파일만이 아닙니다

위 문장을 **파일 경로로 좁게 읽지 마세요.** 자격증명이 들어 있는 모든 저장소가 대상입니다.

- `.env*`, `*.pem`, `*.key`, `*.tfvars` 등 파일
- **OS 키체인 / 자격증명 관리자** (`git credential fill`, `security find-generic-password`,
  `secretstool`, `keyring` 등으로 꺼내는 것 포함)
- `gh auth token`, `aws configure get`, `kubectl config view --raw` 등 **도구를 통해
  토큰을 인쇄시키는 모든 경로**
- 환경변수에 든 토큰을 다른 명령으로 옮겨 쓰는 것

`git push` 가 내부적으로 자격증명을 쓰는 것과, **에이전트가 토큰을 꺼내 다른 호출에
붙이는 것은 다릅니다.** 앞은 도구의 정상 동작이고 뒤는 권한 확대입니다.
값을 출력하지 않고 셸 변수로만 넘기더라도 마찬가지입니다.

### 10-2. 지시받은 수단이 막히면 **멈추고 묻습니다**

사용자가 결과(예: "PR 올려줘")와 함께 수단(예: `gh pr create`)을 지정했는데 그 수단을
쓸 수 없으면, **대체 수단을 스스로 고르지 않습니다.** 막힌 지점과 선택지를 보고하고
지침을 받습니다. 결과에 대한 승인이 수단에 대한 백지위임이 아닙니다.

> 2026-09-16 실제 사례: `gh` 미설치 상태에서 에이전트가 `git credential fill` 로
> 키체인의 GitHub 토큰을 꺼내 API 로 PR 을 생성했습니다. 사용자 자격증명을 사용자에게
> 되묻지 않고 쓴 것이라 인간 의도와 어긋납니다. 올바른 행동은 브랜치 푸시까지만 하고
> (그건 명시적으로 지시받은 범위) "`gh` 가 없습니다. 설치 / 토큰 제공 / 웹에서 직접
> 생성 중 무엇으로 할까요" 를 묻는 것이었습니다.

사후 보고는 동의가 아닙니다. 되돌리기 어려운 일일수록 **하기 전에** 묻습니다.

## 11. 임시 작업 파일 (scratchpad / tasks)

에이전트는 세션마다 `/private/tmp/claude-<uid>/<프로젝트>/<세션id>/` 아래에
중간 산출물을 만듭니다. **사람은 이 경로를 읽지 않습니다.** 그래서 무엇이 쌓이는지,
언제 지우는지를 여기에 적어 둡니다.

| 위치 | 무엇이 들어가나 | 성격 |
| --- | --- | --- |
| `scratchpad/` | 분석 스크립트, 임시 SQL, 빌드 산출물, 가상환경 | 에이전트가 직접 생성 |
| `tasks/*.output` | 백그라운드 명령의 stdout/stderr | 자동 생성 |

### 11-1. 무엇을 넣어도 되고 안 되는가

- **넣어도 되는 것:** 일회성 분석 스크립트, 명령 출력, 리포트 초안, 의존성 해석 결과
- **넣으면 안 되는 것:** 자격증명(§10-1), `.env` 사본, 개인정보.
  API 키를 쓰는 스크립트를 쓸 때는 **값을 파일에 적지 말고** `config.py` 가 읽게 합니다.
- **주의가 필요한 것:** DB 조회 결과. 비밀은 아니지만 팀의 업무 데이터가 평문으로 남습니다.
  재현에 필요한 최소한만 남기고, 표본이면 몇 행으로 자릅니다.

> `/private/tmp/claude-<uid>` 자체는 `0700` 이라 같은 머신의 다른 사용자는 못 읽습니다.
> 다만 백업·동기화 도구가 `/tmp` 를 걷어 갈 수 있고, macOS 의 자동 정리는 **재부팅에
> 의존**하므로 실제로는 몇 주씩 남습니다. "임시" 를 신뢰하지 마세요.

### 11-2. 언제 지우나

| 시점 | 무엇을 |
| --- | --- |
| **task 하나가 끝날 때** | 그 task 에서만 쓴 스크립트·중간 파일. 특히 **가상환경과 빌드 산출물**(수십 MB) |
| **세션이 끝날 때** | 남길 것을 레포로 옮기고 나머지는 정리 대상으로 보고 |
| **프로젝트가 끝날 때** | 해당 프로젝트 폴더 전체 |

**에이전트가 스스로 지우지 않습니다.** 정리 대상과 크기를 보고하고 **사람이 실행할
명령을 제시**합니다(§12 의 "되돌릴 수 없는가" 에 걸립니다).

```bash
# 현재 쌓인 양 확인 (읽기 전용, 에이전트가 바로 실행해도 됨)
du -sh /private/tmp/claude-$(id -u)/*/ 2>/dev/null

# 큰 것부터 (가상환경/빌드 산출물은 재생성 가능하므로 1순위)
du -sh /private/tmp/claude-$(id -u)/*/*/scratchpad/* 2>/dev/null | sort -h | tail

# 정리 — 사람이 직접 실행
rm -rf /private/tmp/claude-$(id -u)/<프로젝트>/<세션id>
```

### 11-3. 남길 가치가 있으면 레포로 옮깁니다

scratchpad 의 분석 스크립트가 다시 쓸 만하면 `scripts/` 나 테스트로 승격시키고,
그렇지 않으면 **결론만 문서에 적고 스크립트는 버립니다.** 임시 폴더에 두면
다음 세션의 에이전트도, 사람도 못 찾습니다.

## 12. 실행 전 자기 점검 (자동 모드에서도)

권한 모드가 자동이어도 **모든 명령이 자동으로 정당해지지는 않습니다.**
이 절이 **0절의 "쉘 명령 실행 전 확인" 을 구체화한 것**이고, 같은 명령에 대해서는
0절보다 우선합니다.

아래 다섯 개 중 **하나라도 '예' 면 실행하지 말고 먼저 묻습니다.**

1. **비밀에 닿는가** — 자격증명 저장소·키 파일을 읽거나, 토큰을 꺼내 다른 호출에 붙이는가 (§10-1)
2. **되돌릴 수 없는가** — 삭제, 덮어쓰기, force push, 외부 전송, 과금 발생
3. **레포 밖으로 나가는가** — 원격 저장소·외부 API·이슈/PR/메시지 등 남에게 보이는 곳
4. **지시받은 수단과 다른가** — 사용자가 도구나 방법을 지정했는데 그것을 바꾸는가 (§10-2)
5. **사용자가 결과를 예측할 수 없는가** — 내가 지금 설명하지 않으면 무슨 일이 일어날지 모르는가

반대로 **다섯 개가 전부 '아니오' 면 묻지 않고 진행합니다.** 읽기 전용 조회, 로컬 분석,
테스트 실행, 레포 안 파일 수정처럼 되돌릴 수 있고 레포를 벗어나지 않는 일에까지
확인을 받으면 자동 모드의 의미가 없습니다.

### 12-1. 판단이 갈릴 때

애매하면 **묻는 쪽**입니다. 묻는 비용은 한 턴이고, 틀렸을 때의 비용은 회수가 안 됩니다.
특히 1번(비밀)에서 "이 경우는 괜찮다" 는 논리를 세우고 있다면, **그 논리를 세우고 있다는
사실 자체가 멈추라는 신호**입니다.

### 12-2. 실행 후에는 무엇을 남겼는지 말합니다

되돌릴 수 있는 일이라도, **임시 폴더에 무거운 것(가상환경, 빌드 산출물, 대용량 출력)을
남겼으면 그 사실과 정리 방법을 보고**합니다. 사람이 안 읽는 경로에 조용히 쌓아 두지 않습니다.

## 참고 문서

- [README.md](README.md) — 프로젝트 개요와 실행 순서
