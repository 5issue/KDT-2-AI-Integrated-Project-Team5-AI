# Database Migration

## 환경변수 설정

`.env.example`을 복사한 뒤 Neon 접속 정보를 입력합니다.

```bash
cp database/.env.example database/.env
```

현재 `migrations/env.py`는 `database/.env`가 아니라 저장소 루트의 `.env` 또는
`data_pipeline/.env`를 읽습니다. 따라서 적용 전 `DATABASE_URL_DIRECT` 또는
`DATABASE_URL`을 프로세스 환경변수로 직접 전달하거나 `env.py`의 환경변수 경로를
확인해야 합니다.

## 마이그레이션 명령

현재 적용된 버전을 확인합니다.

```bash
uv run alembic -c database/alembic.ini current
```

최신 버전까지 적용합니다.

```bash
uv run alembic -c database/alembic.ini upgrade head
```

마이그레이션 목록을 확인합니다.

```bash
uv run alembic -c database/alembic.ini history
```

가장 최근 마이그레이션 하나를 되돌립니다.

```bash
uv run alembic -c database/alembic.ini downgrade -1
```

실제 적용 없이 실행될 SQL만 확인합니다.

```bash
uv run alembic -c database/alembic.ini upgrade head --sql
```

Alembic은 `DATABASE_URL_DIRECT`를 우선 사용하고, 없으면 `DATABASE_URL`을 사용합니다.
DDL 적용에는 pooler endpoint보다 direct endpoint 사용을 권장합니다.
